// 1. Crie um componente chamado PlanetCard dentro da pasta src/components.

// 2. O componente PlanetCard deve receber duas props: uma chamada planetName
// e outra chamada planetImage.

// 3. O componente PlanetCard deve ter uma div que envolva todo seu conteúdo e
// que tenha o atributo data-testid="planet-card".

// 4. O componente PlanetCard deve renderizar o texto recebido pela prop planetName.
// Sugerimos a utilização de tags de Conteúdo de Fluxo, como <p>, que deve conter
// o atributo data-testid="planet-name".

// 5. O componente PlanetCard deve renderizar uma imagem que tenha o atributo src
// com o valor recebido pela prop planetImage.

// 6. Além do atributo src, a imagem renderizada deve ter o atributo alt com o
// texto Planeta {planetName}, onde {planetName} é o valor recebido pela prop planetName.

// O que será verificado
// Será validado se o componente <PlanetCard /> é renderizado.

// Será validado se o componente <PlanetCard /> possui uma div com o
// atributo data-testid="planet-card".

// Será validado se é renderizado o texto recebido pela prop planetName.

// Será validado se é renderizada uma imagem com o atributo src com o mesmo valor
// recebido pela prop planetImage.

// Será validado se, além do atributo src, a imagem renderizada possui o atributo alt
// com o texto Planeta {planetName}, onde {planetName} é o valor recebido pela prop planetName.
